#include "main.h"

/**
 * _abs - gives absolute value of
 * @number: inputted integer
 * an integer
 * Return: int if positive
 * abs value if negative
 */
int _abs(int number)
{
	if (number > 0)
		return (number);
	return (-number);
}
