#include "main.h"

/******************************************************************************
* mul - finds the multiplication of two numbers
* @a: the first number
* @b: the second number
* Return: the multiplication results
*****************************************************************************/
int mul(int a, int b)
{
	return (a * b);
}
